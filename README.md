# Pok-dex-CLI-
a command-line application that lets you explore Pokémon data directly from your terminal. This project uses the public PokéAPI to fetch real Pokémon data via HTTP GET requests and presents it through an interactive REPL (Read–Eval–Print Loop).
